# Grain Module — Python Dependencies
# Install: pip install -r requirements.txt

# PDF extraction (pymupdf preferred; PyPDF2 as fallback)
pymupdf>=1.23       # pip install pymupdf   — best PDF text extraction
PyPDF2>=3.0         # pip install PyPDF2    — fallback if pymupdf unavailable

# Core (from project root requirements.txt — already installed)
# pandas, requests, pyyaml, duckdb, tqdm, click
